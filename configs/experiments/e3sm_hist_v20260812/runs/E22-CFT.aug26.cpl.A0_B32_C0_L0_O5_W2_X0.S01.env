# generated by make_cpl_config.py -- stage-3 coupled fine-tune
# atm parent E01-FT.aug26.atm.A0_B32_C0_L0_O5_W0_X0.S01   (same atm as E18-CFT, E20-CFT)
# ocn parent E13-FT.aug26.ocn.A0_B16_C0_L0_O5_W2_X0.S01   (differs from E18-CFT, E20-CFT)
#
# Completes E01's ocean-weighting triptych: E18=W0 (E11), E20=W1 (E12), this
# is W2 (E13). Against both siblings the config differs in exactly one line
# -- the ocean's parameter_init.weights_path -- so all three isolate the
# ocean seed alone on an identical atmosphere.
#
# The parallel triptych on the CO2 atmosphere (E19=W0, E21=W2) is still
# missing its W1 arm (E02 x E12) -- that is the next open cell in the 2x3
# grid, not this one.
#
# --nodes 8 to match this atm parent's own global batch (B32), per campaign
# convention.
#
# E13-FT (W2) verified healthy before use: 4 best_inference_ckpt.tar saves,
# no rollout divergence. Both realms initialize from best_inference_ckpt.tar,
# matching every other stage-3 run this campaign.
FME_NODES=8
FME_RANKS=32
FME_PRIORITY=1
WANDB_NAME=E22-CFT.aug26.cpl.A0_B32_C0_L0_O5_W2_X0.S01
WANDB_RUN_GROUP=aug26.cpl.E22-CFT
WANDB_JOB_TYPE=A0_B32_C0_L0_O5_W2_X0_CFT
WANDB_TAGS=cft,stage3,aug26,E22,cpl,A0,B32,C0,L0,O5,W2,X0,S01
WANDB_NOTES="stage-3 coupled fine-tune of E01-FT.aug26.atm.A0_B32_C0_L0_O5_W0_X0.S01 (atm) x E13-FT W2 (ocn), both from best_inference_ckpt.tar | completes E01's ocean-loss-weighting triptych (E18=W0, E20=W1, this=W2) | 8 nodes, 32 ranks, 50 epochs"
