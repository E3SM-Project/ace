# generated by make_cpl_config.py -- stage-3 coupled fine-tune
# atm parent E02-FT.aug26.atm.A0_B32_C1_L0_O5_W0_X0.S01
# ocn parent E11-FT.aug26.ocn.A0_B32_C0_L0_O5_W0_X0.S01
#
# B32 in this run id is the *parents'* arm, not this run's batch. The coupled
# stage is pinned to a global batch of 16: make_cpl_config.py derives the node
# count from the 16 ocean inference ICs, which shard across ranks and must
# divide evenly. A genuine B32 coupled run needs 32 ocean stamps regenerated
# against the real 5-day axis (make_stage2_config.py:229), not a padded list.
#
# Both realms initialize from best_inference_ckpt.tar -- rollout skill, which is
# what a coupled model is for -- rather than best_ckpt.tar. Safe here because
# these are stage-2 parents: the monotonic rollout divergence that makes
# best_inference_ckpt meaningless for a *stage-1* checkpoint is exactly what the
# multi-step fine-tune removes. Ocean inference error over epochs 5..35 is
# 0.0537 0.0531 0.0514 0.0540 0.0516 0.0509 0.0511 -- flat, so the min is a real
# selection rather than the luckiest early epoch.
FME_NODES=4
FME_RANKS=16
FME_PRIORITY=1
WANDB_NAME=E19-CFT.aug26.cpl.A0_B32_C1_L0_O5_W0_X0.S01
WANDB_RUN_GROUP=aug26.cpl.E19-CFT
WANDB_JOB_TYPE=A0_B32_C1_L0_O5_W0_X0_CFT
WANDB_TAGS=cft,stage3,aug26,E19,cpl,A0,B32,C1,L0,O5,W0,X0,S01
WANDB_NOTES="stage-3 coupled fine-tune of E02-FT.aug26.atm.A0_B32_C1_L0_O5_W0_X0.S01 (atm) x E11-FT B32 (ocn), both from best_inference_ckpt.tar | 4 nodes, 16 ranks, 50 epochs"
